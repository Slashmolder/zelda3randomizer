# Regression-corpus manifest (tasks.md §12.1).
#
# Each entry: { settings: {<key>: <value>, ...}, seed_u64: <int>, expected_digest: <sha256_hex> }
#
# The corpus is regenerated on every (Linux, macOS, Windows) CI runner against
# the binary at the listed generator_version; any byte-diff in expected_digest
# vs. observed is a CI failure (cross-platform determinism guarantee per
# randomizer-core / Deterministic cross-platform placement).
#
# Switch is a manual gate (not CI-blocking) per tasks.md §12.3a.
#
# **A0 status**: empty. Populated in Phase A1 task 12.2 (50 seeds across the
# world-state × goal × item-pool-difficulty matrix, including 5+ named
# celebrity seeds).
#
# Bump policy: when kGeneratorVersion advances (per tasks.md §13.6),
# tools/bump_rando_corpus.py regenerates digests and updates this manifest.

generator_version: 6  # bumped: BuildItemPool now pads to world-state-filtered location count, not kRandoLocationsCount

# Smoke seeds: exercise the placer + codegen + bump-corpus pipeline end-to-end.
# Each digest is captured against the current `kGeneratorVersion`; when the
# placer / RNG / settings serialization / logic graph changes, bump the version
# and regenerate via `python assets/scripts/bump_rando_corpus.py --apply`.
entries:
  - settings: { mode.state: open, goal: fast_ganon, crystals.ganon: 7, crystals.tower: 7, dungeon_items.small_keys: dungeon }
    seed: "0xDEADBEEFCAFEBABE"
    label: "a1-open-fast-ganon"
    expected_digest: "4570a1679d32e4565024753583e1d7a046e28f2ec978fe5a762569f7213db46d"
  # 7-goal × 4-world-state × dungeon-mode matrix — captured against
  # kGeneratorVersion=4 with the vanilla-mode pre-grant fix landed.
  # All entries verified goal_completable=true at capture time.
  - settings: { mode.state: open, goal: fast_ganon }
    seed: "0x0000000000000001"
    label: "a1-open-fast-ganon-default"
    expected_digest: "9bb88253c21cc0ee1b870aeb2e4d754fa4fecf6028f9c25f0649c14e224b9665"
  - settings: { mode.state: open, goal: dungeons }
    seed: "0x0000000000000002"
    label: "a1-open-dungeons"
    expected_digest: "3258f9808656f736acc350c70faa40ff031d21e35f031e16b3dc1663d94a976a"
  - settings: { mode.state: standard, goal: fast_ganon }
    seed: "0x0000000000000003"
    label: "a1-standard-fast-ganon"
    expected_digest: "3150e1052c4fa0886dd44b2288637dfd6f2222bf6ef9b0728b5db31df930586b"
  - settings: { mode.state: open, goal: pedestal }
    seed: "0x0000000000000004"
    label: "a1-open-pedestal"
    expected_digest: "b50bf3a75c25d87836bd844e1096860f54bc33b693eb492748d75f42517cead9"
  - settings: { mode.state: open, goal: "triforce-hunt", pieces_required: 20, pieces_placed: 30 }
    seed: "0x0000000000000005"
    label: "a1-open-triforce-hunt"
    expected_digest: "578a9b66cc756711e5c0642d60bf7203cc3101feea3fe966424d2f0cde78c52e"
  - settings: { mode.state: open, goal: ganonhunt, pieces_required: 20, pieces_placed: 30 }
    seed: "0x0000000000000006"
    label: "a1-open-ganonhunt"
    expected_digest: "bd752a50b0f472a4ecef3dc989f4f8b2bab255ec4d5e75a039eaf8460486ecdf"
  - settings: { mode.state: open, goal: completionist }
    seed: "0x0000000000000007"
    label: "a1-open-completionist"
    expected_digest: "cc54227df902cc2e90227d37fef895dc1fc21f7702ec8817bf3af17ef620782a"
  - settings: { mode.state: open, goal: fast_ganon, dungeon_items.small_keys: dungeon, dungeon_items.big_keys: dungeon, dungeon_items.maps: dungeon, dungeon_items.compasses: dungeon }
    seed: "0x0000000000000008"
    label: "a1-open-dungeon-keys-all"
    expected_digest: "dde44adaa1d08c7c0d2ce8b61b42a2f73cababd40e17dcac637e4c509893a274"

# Celebrity-seed candidates (task 12.2) — these are the named seeds we plan
# to capture once the generator stabilizes. Listed here as comments so the
# next session can populate them in order. Each MUST include `seed` as a
# YAML *string* (per randomizer-core / CLI manifest schema — YAML 1.1 vs 1.2
# uint64 parsing portability).
#
#   - settings: { mode.state: open, goal: dungeons, item_pool_difficulty: normal }
#     seed: "0xTBD"
#     label: "all-bosses-12-min"
#     expected_digest: "<filled-after-first-run>"
#
#   - settings: { mode.state: open, goal: fast_ganon, mode.weapons: swordless }
#     seed: "0xTBD"
#     label: "swordless-skip-mire"          # Phase B (swordless mode)
#     expected_digest: "<filled-after-first-run>"
#
#   - settings: { mode.state: open, goal: "triforce-hunt", pieces_required: 25, pieces_placed: 30 }
#     seed: "0xTBD"
#     label: "triforce-hunt-25-of-30"
#     expected_digest: "<filled-after-first-run>"
#
#   - settings: { mode.state: standard, goal: ganonhunt }
#     seed: "0xTBD"
#     label: "standard-ganon-hunt"
#     expected_digest: "<filled-after-first-run>"
#
#   - settings: { mode.state: open, goal: completionist }
#     seed: "0xTBD"
#     label: "completionist-open"
#     expected_digest: "<filled-after-first-run>"
