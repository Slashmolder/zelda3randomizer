# Tasks — add-jp-glitch-restoration

> **RESUME NOTE 2026-06-08 (updated after the §5.3 deeper spike).**
> Implementation branch merged with main (enemy-shuffle base + reconciled specs). Default-off feature; **no rando/corpus/kGeneratorVersion impact.** WSL `make -j zelda3` (-Werror) green (UI included; no new .c -> no vcxproj change). The stale "8-frame grace" framing is gone (merge pulled in the reconciled specs).
> **DONE:** flag `kFeatures0_RestoreJpGlitches` (bit 18) + INI key `RestoreJpGlitches` + UI checkbox; helper `JpGlitchEnabled()` = `(enhanced_features0 & flag) && !ZeldaIsEmulatorAttached()`.
> **ALL 6 cataloged JP-1.0 glitches now restored** behind the one flag: **Fake Flippers** (§3.3, confirmed), **Death Hole** (§5.1), **Itemdash** + **Spindash** + **Superspeed** (§5.2/§5.5 — the SAME single StartDash guard gates the whole item+sword block; Spindash/Superspeed playtest-CONFIRMED), **Mirror Block Erase** (§5.4, found by the §5.3 spike). **ALL SIX playtest-CONFIRMED** (Fake Flippers, Death Hole, Itemdash, Spindash, Superspeed, Mirror Block Erase). Feature-complete.
> **DECISION PENDING (owner):** squash-merge to main (default-off; like the enemy-shuffle MVP) — optionally after a Death Hole playtest. The §5.3 deeper spike is complete and the glitch set is feature-complete.

## 1. Discovery spike — JP-vs-US disassembly diff (the "find what's patched" half)

- [x] 1.1 Done — JP-vs-US 65816 ROM diff (relocation-normalized) across the swim/pit/dash/spin/block routines, using the C source's `// <addr>` comments as the address oracle. Both ROMs headerless 1 MB LoROM; the code banks are separately re-linked (cumulative shift), so routines were signature-located and diffed by logic, not raw offset.
- [x] 1.2 Mapped each delta to its `src/*.c` site (all three in `player.c`: swim handler, pit-fall loop, ground dispatch).
- [x] 1.3 Findings: **3 IMPLEMENTABLE** (exact US-added instruction, cleanly gatable) — Fake Flippers (per-frame flipper recheck in `PlayerHandler_04_Swimming`), Death Hole (`link_disable_sprite_damage++` per pit-fall frame), Itemdash (StartDash guard skipping `Link_HandleYItem`). **3 NEEDS-MORE-WORK** (no JP↔US code delta in the anchored routines — state-leak / input-race, likely not reproducible in fixed C control flow): Superspeed, Spindash, Mirror Block Erase.
- [x] 1.4 Confirmed each delta is US-ADDED (present in US ROM, absent in JP), so the US-faithful port carries the fix; gating it restores JP behavior.

## 2. Flag + config plumbing

- [x] 2.1 Add `kFeatures0_RestoreJpGlitches = 262144` (bit 18) to the `kFeatures0_*` enum in `src/features.h`, with a comment noting it is behavior-affecting + default-off + side-by-side-off.
- [x] 2.2 Wire a new named `[Features]` boolean key (e.g. `RestoreJpGlitches`) in `src/config.c`: a `ParseBoolBit(value, &g_config.features0, kFeatures0_RestoreJpGlitches)` branch (`:615-646`), aligned entries in `kFeatKeys[]` + `kFeatMasks[]` (`:1193-1208`, guarded by their `_Static_assert`), and a default-off. (There is no `[Features] features0` integer mask — do not edit one.)
- [x] 2.3 Verify the bit round-trips through `zelda3.ini` (write → re-parse → identical) per `game-config-ui / Round-trip fidelity`. (By construction: the new key is in `kFeatKeys[]`/`kFeatMasks[]` so the comment-preserving writer emits it and the parser reads it back through the same mask. Build-verified the tables stay aligned via the existing `_Static_assert`; an end-to-end write/re-parse playtest remains.)

## 3. Fake Flippers (FAITHFUL — implemented)

- [x] 3.0 Grounding spike DONE. ROM truth: there is **no 8-frame grace counter** in JP. The swim-ENTRY ejects (`CheckAbilityToSwim`, `LinkState_CrossingWorlds`) are byte-identical JP↔US; the ONLY delta is the per-frame flipper recheck US added to `PlayerHandler_04_Swimming`.
- [x] 3.1 Gated EXACTLY that one site under `JpGlitchEnabled()` = `(enhanced_features0 & kFeatures0_RestoreJpGlitches) && !ZeldaIsEmulatorAttached()`. **REVERTED** the earlier over-gating of the two entry sites — they are identical JP↔US, and gating them caused the "always-swim" bug the owner rejected. Walk-into-water still ejects; flipperless swim only via the un-flipper-checked ledge/recoil entry, exactly as JP 1.0.
- [x] 3.2 FAITHFUL — no approximation. The free-swim fallback is removed; this IS the JP behavior byte-for-byte (the C handler is now equivalent to JP's).
- [x] 3.3 Playtest (owner) **CONFIRMED 2026-06-08**: with the flag on, the owner must jump into water and trigger a screen transition immediately to lock the swim state — exactly JP 1.0; walking straight into water still ejects. Matches the single-site-gate model. (Flag-off + ROM-attached clean-compare not separately re-checked, but the gate is `!ZeldaIsEmulatorAttached()`-guarded by construction.)

## 4. UI

- [x] 4.1 Added the "Restore JP 1.0 glitches" checkbox to the gameplay-feature panel in `src/rando/rando_window/game_config_panels.cpp` (PC-only, `Z3R_NATIVE_SETTINGS_WINDOW`), bound to the bit via the existing `FeatureCheckbox` helper, one-line durable tooltip ("Re-enables glitches removed in the US 1.0 release.") + a `TextDisabled` active-set/approximation line.
- [~] 4.2 Live-apply is inherited from the shared `features0` path (the `FeatureCheckbox` helper writes `s_cfg.features0`, which the existing apply flow mirrors into `enhanced_features0` per-frame). Switch-exclusion is by construction — the whole `rando_window/` panel set is `Z3R_NATIVE_SETTINGS_WINDOW`-only. The WSL `make` build (which DOES define `Z3R_NATIVE_SETTINGS_WINDOW=1`) compiled the checkbox cleanly. An interactive live-apply check on PC remains a playtest item.

## 5. Follow-on glitches

- [x] 5.1 Death Hole (FAITHFUL): gated the US-added `link_disable_sprite_damage++` per pit-fall frame (`player.c`) under `JpGlitchEnabled()`. Code is byte-faithful to JP; the full warp needs a same-frame death+pit-fall input. **Playtest-CONFIRMED (owner).**
- [x] 5.2 Itemdash (FAITHFUL): relaxed the US-added StartDash guard so `Link_HandleYItem` fires on the dash-start frame under `JpGlitchEnabled()` (`player.c`). Enabling delta is exact; per-item payloads **playtest-pending** (owner).
- [x] 5.3 **Deeper spike DONE** (JP-vs-US relocation-normalized + lockstep 65816 ROM diff; custom differ, ROMs at the paths in the handoff). Surfaces diffed: **input-latch** (`Interrupt_NMI` `$80:80C9`, `NMI_ReadJoypads` `$80:83D1` — IDENTICAL, shift +0 → same-frame input-race glitches are NOT an input-filter delta); **control/player routines** (`PlayerHandler_00_Ground_3` `$87:81A0` — only delta is the already-implemented Itemdash guard; `Player_Sword_SpinAttackJerks_HoldDown`, `Link_ActivateSpinAttack`, `LinkState_Dashing` — all IDENTICAL); **transition / speed-reset** (`PreOverworld_LoadProperties` `$82:83C7`, `Dungeon_ResetTorchBackgroundAndPlayerInner` `$82:8B0C` — `link_speed_setting` reset present in BOTH); **mirror/block** (`Dungeon_PushBlock_Handler` trivial STX/STY only; `PushBlock_CheckForPit` IDENTICAL). Incidental off-target delta noted: `Link_HandleBunnyTransformation` has a JP-only state-reset call (`$87:F18C`), and `LinkItem_Mirror` differs only in the follower-10 "can't mirror" message — neither is one of the three glitches.
- [x] 5.4 **Mirror Block Erase (FAITHFUL — IMPLEMENTED).** The delta is in `DoSwordInteractionWithTiles_Mirror` (`$87:A95C`) indoor path: JP 1.0 clears the changeable-dungeon-object indices (`index_of_changable_dungeon_objs` `$5FC/$5FD`) **unconditionally and BEFORE** `Mirror_SaveRoomData` → `Dungeon_FlagRoomData_Quadrants`, so a mid-push block is flagged absent and erased on the mirror reload. US 1.0 reordered the save ahead of the now-`sound_effect_1!=60`-gated clear, fixing it. **Proven**: JP's `$82:A0A8` == US `Mirror_SaveRoomData` (`$82:A1B1`), relocation-only (lockstep, RTL at instr 19). Gated the exact JP order under `JpGlitchEnabled()` in `src/player.c`; the else-branch is byte-identical to US 1.0. WSL `-Werror` green. **Playtest-CONFIRMED (owner).** The prior spike missed this — it anchored on push-block motion + the design's stale "mirror dispatch :2107" line, not `DoSwordInteractionWithTiles_Mirror`.
- [x] 5.5 **Superspeed / Spindash: SAME ROOT CAUSE as Itemdash — playtest-CONFIRMED, no extra code.** The §5.3 sweep (player.c 201 + dungeon.c 363 + overworld.c 159 = 723 routines, relocation-normalized + lockstep) found NO separate delta because there isn't one: the **single US-added StartDash guard** in `PlayerHandler_00_Ground_3` (`$87:81C0`, `LDA $5D / CMP #$11 / BEQ +$19`) skips the **entire item-AND-sword block** on the dash-start frame — its `BEQ +$19` jumps over **both** `JSR $9B0E` (`Link_HandleYItem`) **and** `JSR $9AC2` (`Link_HandleSwordCooldown`, the `button_b_frames` spin-charge pipeline). One guard, three glitches: Y-item = **Itemdash** (Hookdash/Medalliondash arm Superspeed); spin pipeline = **Spindash**; the armed `link_speed_setting==0x10`+walking-control state either leaves, activated by walking off a manual staircase = **Superspeed**. The one `JpGlitchEnabled()` bypass (task 5.2) restores all three. **Owner playtest-CONFIRMED Superspeed + Spindash both work.** Stairs handlers carry NO `$5E`/`$372` reset delta (research's "stairs-exit $5E reset" hypothesis disproven); the movement state machine is byte-identical JP↔US apart from this one guard. **All six cataloged JP-1.0 glitches are now restored.**

## 6. Docs + audit

- [~] 6.1 Partial: the active set (Fake Flippers, Death Hole, Itemdash — all JP-faithful) is documented in-code (the `JpGlitchEnabled` doc block enumerates each glitch's exact JP↔US delta) and in the UI ("Currently: Fake Flippers, Death Hole, Itemdash (JP-faithful)"). A standalone `docs/` feature page + per-glitch active-set table is deferred to the follow-on.
- [x] 6.2 No rando impact: the code diff touches only `src/config.c`, `src/features.h`, `src/player.c`, `src/rando/rando_window/game_config_panels.cpp` — `RandoSettings` / canonical layout / `kGeneratorVersion` / corpus all untouched (verified via `git diff --stat main...HEAD`).
- [x] 6.3 Fresh-eyes audit pass DONE (pre-merge, parallel general-purpose agent) — **no material findings.** Verified: every flag-off path reduces byte-identically to US code (incl. the Mirror Block Erase `else`-branch = original `Mirror_SaveRoomData(); if(sound!=60) clear;`), `kFeatures0_RestoreJpGlitches` bit 18 is collision-free with consistent `kFeatKeys`/`kFeatMasks`/`_Static_assert` plumbing, the StartDash guard reduces to the original `!= kPlayerState_StartDash`, no rando dispatch/grant/placement interaction, and `!ZeldaIsEmulatorAttached()` is centralized in `JpGlitchEnabled()` (no gate omits it). Clean to squash-merge.
- [x] 6.4 Documented the unsupported combination (rando `fake-flippers` placement trick + this runtime flag OFF → assumed swim not executable → soft-softlock risk; no auto-coupling) in `docs/randomizer.md` under "Tricks & glitch logic".
