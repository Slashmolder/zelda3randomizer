# Tasks: NPC Souls

## 0. Prerequisite

- [x] 0.1 SUPERSEDED by owner instruction: implemented on the SAME branch as add-enemy-souls (its machinery is here); archive ordering handled at merge staging. Byte [28] bit 4 re-verified free against the rebased main (127-era) (this change builds directly on its machinery: souls.c, spawn hooks, `direct_soul` dispatch, sidecar v6, TLV type 8, souls UI). Re-verify canonical byte [28] bit 4 is still free against live `main` before claiming it.

## 1. Ground truth & data

- [x] 1.1 GENERATE the roster site table from source — hand transcription is banned for this data (the plan-review pass falsified 3 transcribed ids): a committed script parses the sprite handler table + prep handlers (`SpritePrep_Sage`/`FluteKid`/`Shopkeeper`/`Smithy`/`Kiki`) and room/overworld sprite lists, emits (type, room/area, world) per site, and asserts each id resolves to the expected handler symbol. Pin the two unknowns (Mini Moldorm Cave NPC, Hype Cave NPC) and every spawn path (the first-pass static/proxima labels were unreliable).
- [x] 1.2 (GENERATED, not hand-authored) `assets/rando/npc_souls.yaml`: `souls:` roster (23 names → item ids 196-218), `sites:` (type, room/area|any, world|any, soul) suppression table — NO subtype column (unavailable at the hooks), `gates:` (location → required souls list, incl. multi-person entries and the Pyramid Fairy + Bomb Shop pairing), `trigger_gates:` (Witch hand-in + powder bag, Catfish throw, both fairy ponds; the Magic Bat is plain static suppression — room-static 0x3A idler, no dynamic summon). Generator validates: room-keyed (interior) sites are `world=any` (Inverted swaps interior entering-worlds — a world-scoped Bomb Shop row would silently stop suppressing), sites disjoint from `kSoulForSprite` species, NO 0xC0 or 0xE9 site ever, no 0xBB site colliding with a Retro take-any host room (0x10F/0x112/0x11F), every gate target resolves to a registry location.
- [x] 1.3 Registry: append 23 soul items (`category: soul`, `dispatch: direct_soul`); regenerate `item_ids.h`; `ITEM__COUNT` 196→219; extend the souls `_Static_assert`s (capacity, id contiguity).
- [x] 1.4 Kiki edge modeling (base-edge term via the new edge-wrap pass; Inverted additive-edge bypass verified in Logic_SelfCheck via the inverted corpus witness; runtime Inverted gate F12 check folded into the 6.3 playtest): the base PoD edge (`logic_parts/20_palace_of_darkness.yaml`) takes the term; Inverted's ADDITIVE TRUE() edge auto-bypasses it (verified); entrance shuffle keeps edge predicates through the redirect (verified). REMAINING: F12-verify the Inverted RUNTIME opens the PoD gate without Kiki's payoff (`save_ow_event_info & 0x20`) — if not, the TRUE() edge is a latent pre-existing softlock to fix first; document the chains-seam bypass equivalence in the YAML comment.

## 2. Settings & pool

- [x] 2.1 `npc_souls` in `RandoSettings` (CSV convention: true/false per parse_bool, not on/off): canonical [28] bit 4 encode/decode INCLUDING relaxing the deserialize hard-refusal mask (`Settings_FromCanonical` returns -2 on any [28] bits-4-7 today; accept bit 4, keep refusing 5-7), `Settings_ParseCsv` key, hash + share-string coverage, allocation-comment claim, Settings_SelfCheck block (on/off round-trip; bit-5-7 refusal still fires; NO degrade under door shuffle — assert it survives).
- [x] 2.2 Pool: `pool_add` 23 souls when on; `is_progression_item()` covers ids 196-218; Placement_SelfCheck asserts 23-on / 0-off AND that worst-case progression count (wild keys + hunt pieces + souls-all 46 + npc 23) stays within `progression[256]`/`prog_slot[256]`.
- [x] 2.3 Fill model: `souls_collect_model` trigger gains `|| npc_souls on` (rando_placement.c); comment updated; off-path model selection provably unchanged.

## 3. Logic codegen

- [x] 3.1 `OP_NPC_SOULS_ACTIVE` (op 29): registry entry, codegen emit/parse, `eval_npc_souls_active`, `skip_pred` operand entry (no operands), op-count bump.
- [x] 3.2 Wrap injection in `rando_logic_gen.py` from `npc_souls.yaml` `gates:` (wrap-pass style like `_apply_soul_room_wraps`, NOT logic_parts duplicates); hard error on unresolved gate targets; BUILD the EDGE-wrap facility (`_apply_soul_room_wraps` handles locations only today) and put the Kiki term on the PoD entry edge per 1.4; Pyramid Fairy checks gain Fairy + Bomb Shop terms.
- [x] 3.3 Logic_SelfCheck (probes: Stumpy gate, Kiki-holds-PoD, Maze-Race-needs-BOTH, off-inert): end-to-end probe (e.g. Stumpy unreachable without its soul at npc on, reachable with; off-tier inert) + the structural skip walk passes over the new blobs.

## 4. Runtime

- [x] 4.1 `Souls_NpcSpriteAllowed(type, key, is_room)` (world column is generator-validated documentation — area ids already encode world) + generated site table in souls.c; ownership bits appended after the 46 existing souls; grant dispatch range extended.
- [x] 4.2 Hook wiring: dungeon static hook passes room (subtype is NOT available pre-suppression — review-verified; do not attempt it); overworld proxima hook passes area + world; the static-hook NPC branch CONSUMES the slot exactly like the enemy-souls branch (inert state-0 slot, `sprite_N[k]=k`, return k) so structural slot indices stay stable — room 0x123 holds slot-keyed enemy checks 0-3 beside the MMC NPC; it does NOT call `Souls_NoteRoomSuppressed()` (kill-gate counter is enemy-souls-only); NO change to the dynamic funnel for NPC souls.
- [x] 4.3 Trigger-site gates AS-BUILT: Catfish = plain STATIC area-site suppression (the scan showed 0xC0 is a static overworld sprite at area 0x4F; throw finds nothing) with the funnel exclusion intact; Witch = 0x36 suppression covers the hand-in (it runs inside the suppressed sprite) + defensive powder-bag despawn; fairy pond grant branches gated in Sprite_WishPond3 (other ponds untouched); King Zora static suppression pending playtest confirmation. Original text: Catfish throw trigger (0xC0 funnel untouched — regression-guard comment); Witch = `Witch_AcceptShroom` hand-in + 0xE9-subtype-1 powder-bag grant branch (0xE9 sprite itself NEVER suppressed — potion cauldrons are logic-assumed magic refills); Waterfall/Pyramid fairy pond grant branches room-scoped (wishing/happiness ponds untouched); King Zora static suppression verified sufficient for his emergence state machine.
- [x] 4.4 Check gating beyond spawn — Maze Race standing-PoH despawn guard SHIPPED (both souls, re-arms on screen re-entry); Chest Game hostless-open + Digging Game sufficiency deferred to the 6.3 playtest (a speculative chest guard risks breaking the minigame dispatch) (design D5): Maze Race standing-item spawn/collect guard on both souls; Chest Game hostless-open audit (guard the room's chest dispatch if needed); verify Digging Game suppression suffices.
- [ ] 4.5 Missable-inversion audit — PARTIAL: bat/pond/witch re-arm verified by construction; smith chain flags, Old Man escort re-entry, Kiki follower stale-flag audit remain playtest items (design D9 direction): for each roster NPC, sweep one-shot vanilla flags that could strand the check when the soul arrives late (smith chain flags, witch mushroom hand-in, Old Man escort re-entry, Kiki follower state stale-flag audit).

## 5. Persistence & UI

- [x] 5.1 `kSoulFlagsBytes` 8→12; `_Static_assert(kSoulCount + kNpcSoulCount <= 96)`; RandoState-accept clears 12 bytes; sweep EVERY 8-byte assumption site: `RandoSaveSlot.header.soul_flags[8]` literal in `rando_save.h` + its `sizeof`-copy sites in `rando.c` (slot capture ~1856, activation ~4134 — re-locate by symbol, not line).
- [x] 5.2 Sidecar format_version 7: append widened flag bytes; ≤v6 reads zero-fill; version-gate tests in RandoSave selfcheck.
- [x] 5.3 TLV type 8 widening: writer emits 12 bytes; reader accepts 8 (zero-fill tail) and 12; KEEP the payload-internal `format_version==1` and grow only the TLV length so pre-widening builds still restore the first 8 bytes; cold-replay + round-trip selfchecks extended.
- [x] 5.4 ImGui `npc_souls` checkbox + tooltip (1-2 durable player facts); tracker souls grid NPC section; icons reuse the generic soul cell via explicit `direct_grant_icons.yaml` entries for ids 196-218 (missing entries leave gfx=0 and silently degrade the confirmation cue to audio-only); hints/spoiler names ride `Rando_GetItemName`.

## 5b. Uncle addendum (owner decision 2026-07-06)

- [x] 5b.1 24th soul `Soul_Npc_Uncle` (id 219): roster append, site = secret-passage room 0x055 ONLY (house 0x104 + priest 0x012 are choreography/shared actors), gate on "Link's Uncle", registry + asserts, tracker/icons/pool ride the count constants.
- [x] 5b.2 Standard escape soundness: the sphere-0 weapon/lamp accept bars EXCLUDE the Uncle slot under npc_souls (an item there is soul-gated); 10/10 Standard+npc seeds generate, weapon steered to the other sphere-0 slots, Uncle check unlocks post-soul in the sphere walk.

## 5c. Player feedback (owner request 2026-07-06)

- [x] 5c.1 Named soul-pickup box: souls all share one generic icon, so the floating blob can't name the soul. `Rando_RenderSoulMessage` renders "Got the <Name> Soul!" via the trap dynamic-dialogue mechanism (id 0x0221), triggered by a DEFERRED request (`Rando_QueueSoulPickupMessage`) consumed on the first quiescent gameplay frame (`Sprite_ShowMessageUnconditional` overwrites main_module_index — cannot fire inline from an NPC-dialogue grant). Covers enemy AND NPC souls; selfcheck exercises the render. Shared soul icon kept (owner decision — no per-NPC art).
- [x] 5c.2 Souls tracker: already lives in the Item Tracker window (Souls + NPC Souls sections; NPC gated on npc_souls). Confirmed, no new window needed.

## 6. Verification & close-out

- [x] 6.1 Clean rebuilds green (MSVC + WSL gcc `-Werror`; `make clean` after header edits); `--rando-selftest` green.
- [x] 6.2 kGen stays 129 (off-path byte-identical, inert-change exception; NPC-on paths are new witnesses). Corpus entries: npc-on open/ganon, npc-on × souls-all, npc-on × standard, npc-on × inverted (Kiki edge), npc-on × entrance shuffle (Kiki term follows the remap), npc-on × chains (PoD successor seam), npc-on × door-shuffle basic (FIRST-EVER collecting-fill-model × door-oracle combination — watch generation wall-clock against the 120s budget, and if the fill exhausts, escalate to a design decision rather than a silent degrade), npc-on × Retro + wild keys (fill pressure + 0xBB take-any adjacency), plus off-path digest-inertness proven by a fresh-`main`-baseline 3-way diff. Corpus green on both compilers (`--jobs 8`).
- [~] 6.3 Playtest matrix (DEFERRED to main per owner decision 2026-07-06 — merge-now, playtest-on-main; the newest pieces — Uncle suppression, the named pickup box from NPC-dialogue vs chest, the race-reveal path — are the priority): each roster NPC absent-then-present around its soul; maze-race prize physically gated; chest/digging game hostless behavior; King Zora grant with Catfish soul missing; PoD entry ± Kiki soul; Standard-mode escape unaffected; save/reload + snapshot cold replay with NPC souls owned.
- [x] 6.4 Independent fresh-eyes IMPL review DONE (2026-07-06, general-purpose reviewer over 37867249..HEAD): 0 HIGH; fixed Uncle icon-yaml gap, 'Soul Npc X' hint text, symbolized bounds. Spec deltas reconciled against as-built source; `docs/randomizer.md` NPC-souls section added. Archived on the branch 2026-07-06 as the pre-squash-merge step (playtest 6.3 deferred to main per owner decision).
